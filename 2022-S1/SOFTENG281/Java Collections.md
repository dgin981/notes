Deterministic: same reponse with same inputs

vertices in the set
tuples represent edges between the vertices

Collection interface
Map interface

$B \subset A$
B is a subset of A
all values of B are in A
